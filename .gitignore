
# internal claude state (do not track) — except the shared deny-list, which must reach every machine
.claude/*
!.claude/settings.json

# gstack local tooling state
.gstack/

# ── Repo is PUBLIC (GitHub Pages). Nothing below may be committed. ──

# Course build sources: digests anchor to an internal production system and to
# copyrighted textbooks. Kept locally so courses can be re-assembled; never published.
course-sources/

# Dev-server + tooling output: leaks absolute paths, LAN IPs, stack traces
logs/
*.log
.playwright-mcp/
app/test-results/
app/playwright-report/

# Local screenshots / scratch captures
login-logo-check.png
*-check.png
screenshot*.png

# Secrets
.env
.env.*
!.env.example

# per-machine Claude Code allowlist (the shared deny list is .claude/settings.json)
.claude/settings.local.json
